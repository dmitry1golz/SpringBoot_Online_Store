spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/store?createDatabaseIfNotExist=true
    username: root
    password: IRof1Jon7Ohf4Rm7

stripe:
  apiUrl: https://api.stripe.com
  enabled: true
  timeout: 3000
  supported-currencies: EUR, USD, GBR

payment-service: stripe

mail:
  host: https://smtp.gmail.com
  port: 5761